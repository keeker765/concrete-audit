# ============================================================
# 混凝土试块造假识别 — SuperPoint + LightGlue 依赖清单
# ============================================================
# 安装步骤:
#   pip install -r requirements.txt
#
# Mac (Apple Silicon) 注意事项:
#   1. PyTorch 直接 pip install，自动选择 CPU/MPS:
#      pip install torch torchvision
#
#   2. pyzbar 需要先安装系统库（QR 识别用）:
#      brew install zbar
#      然后: pip install pyzbar
# ============================================================

# ── 核心深度学习 ──────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0

# ── 图像处理 ─────────────────────────────────────────────
opencv-python>=4.8.0

# ── 特征匹配 (SuperPoint + LightGlue) ────────────────────
lightglue>=0.1.0

# ── QR 码识别 (pyzbar 为可选 fallback) ───────────────────
# Mac 需先: brew install zbar
pyzbar>=0.1.9

# ── Web 服务 ─────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
